# Extractor area

This directory will later hold:

- custom event-sink plugin source
- raw record and segment manifest contracts
- extraction supervisor code/scripts
- S3 upload helpers for sealed segments
- any tiny utilities needed to coordinate bounded replay

Block 01 only freezes the filesystem shape.
