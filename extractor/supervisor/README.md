# Extraction supervisor placeholder

Expected later responsibilities:

- start/stop FullNode with `--es`
- track run status
- rotate segments safely
- resume after interruption
- mark run completion boundaries
